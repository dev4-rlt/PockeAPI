export type PostAbility = {
    name: string;
    description: string;
}
export type PostMove = {
    name: string;
    description: string;
}

export type PostLocation = {
    name: string;
}

export type PostGame = {
    name: string;
}